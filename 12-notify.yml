- name: installing nginx
  hosts: local
  become: yes
  tasks:
  - name: installing nginx
    ansible.builtin.package:
      name: nginx
      state: present

  - name: printing msg
    ansible.builtin.debug:
    notify: restarting


   