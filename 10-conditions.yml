- name: conditions
  hosts: web
  tasks:
  - name: conditions
    ansible.builtin.command: id expense
    register: USER
    ignore_errors: true

   

  - name: printing THE VALUES
    ansible.builtin.debug: "{{USER}}" 
      