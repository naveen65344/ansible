- name: conditions
  hosts: web
  become: yes
  tasks:
  - name: conditions
    ansible.builtin.command: id expense
    register: USER
    ignore_errors: true

  - name: Print 
    ansible.builtin.debug:  
      msg: "{{USER}}" 



  - name: user adding
    ansible.builtin.command: useradd expense
    when: USER.rc !=0         

   

  